The building blocks of all web pages are HTML, CSS and JavaScript.

HTML is a markup language and is used to tell the browser what the structure and content of a web page are.

HTML consists of different elements, which define how the content of the page behaves. 

An HTML element is created using tags.